I want you to flip a coin and execute it using a python file and tell me what you got tail or head 

Try to make tool calls dont make xml files that doesnt invoke toolcall, If you only call `write_file` and stop, the task is incomplete.

**Hard requirements (do not skip):**

**Write** the file that replicates the action of flipping a coin and then prints the result.
   - **Do not use `eval`, `exec`, or `compile` on the input.**

Do **not** narrate a todo list as plain text — use your tools (write_file, execute, …), not prose that looks like tool names. YOU MUST HAVE TO EXECUTE THE TOOL IN THE BACKEND I PROVIDED"

write files in **tmp** folder.

Wait for write_file to finish before applying execute **important**

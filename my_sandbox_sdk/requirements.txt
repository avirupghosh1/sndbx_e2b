# My Sandbox SDK - Requirements
# Core SDK has NO external dependencies - uses Python standard library only

# Development dependencies (optional)
pytest>=7.0
pytest-asyncio>=0.20
mypy>=1.0
black>=23.0
isort>=5.0
flake8>=6.0

# Optional: For better HTTP support (if needed in future)
# requests>=2.28.0
# aiohttp>=3.8.0
# httpx>=0.23.0
